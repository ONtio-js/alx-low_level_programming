#include <stdio.h>
#include <stdlib.h>
/**
 * main - return a certain value of number
 * Return: return 0if successful
 */

int main(void)
{
	int num = '0';
	int comma = ',';

	while (num <= '9')
	{
		putchar(num);
		if (num < 9)
		{
			putchar(comma);
		{
		putchar(' ');
		num++;
	}
	return (0);
}
