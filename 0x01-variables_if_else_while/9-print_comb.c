#include <stdio.h>
#include <stdlib.h>
/**
 * main - return a certain value of number
 * Return: return 0if successful
 */

int main(void)
{
	int num = 9;
	int comma = ',';

	while (num <= 9)
	{
		if (num ==9')
		{
			putchar(num);
		}
		else
		{
			putchar(num);
			putchar(comma);
		}
		num++;
	}
	putchar('\n');
	return (0);
}
