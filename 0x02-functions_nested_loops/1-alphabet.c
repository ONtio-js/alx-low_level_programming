#include "main.h"
/**
 * main - prints the alphabets in lower case
 * Return: returns 0 if successful
 */

int main(void)
{
	char alphabet = 'a';

	while (aphabet <= 'z')
	{
		-putchar(alphabet);
		alphabet++;
	}
	_putchar('\n');
	return (0);
}
